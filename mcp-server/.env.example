# Base URL of the Go Fiber API (no trailing slash)
TK_AQILA_API_BASE_URL=http://127.0.0.1:3000

# JWT from POST /api/admin/login (required for protected tools:
# pendaftaran_list, pendaftaran_get, laporan_periode, jadwal_list)
TK_AQILA_API_JWT=
